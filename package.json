{
  "name": "apikeyvalidator",
  "version": "1.0.0",
  "private": true,
  "description": "APIKeyValidator API key validator, AI model checker, and bug bounty report generator.",
  "scripts": {
    "start": "node server.js",
    "test": "node tests/providerRegistry.test.js && node tests/chatStore.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
